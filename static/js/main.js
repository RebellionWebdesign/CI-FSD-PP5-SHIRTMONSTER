/* This snippet is required to render the Lucide Icons -> https://lucide.dev/guide/packages/lucide */
lucide.createIcons();

/* Custom settings for the bootstrap carousel */
$('.carousel').carousel({
    interval: 5000,
    touch: true,
})